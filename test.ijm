/**
 * Xing Chen (email)
 * 20190623
**/

run("Cell Colony (31K)");
setAutoThreshold("Default dark");
//run("Threshold...");
//setThreshold(172, 255);
setOption("BlackBackground", true);
run("Convert to Mask");
run("Close");
